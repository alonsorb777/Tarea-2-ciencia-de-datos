def mediana(datos):
    """
    calcula la mediana.
    """
    do = sorted(datos)
    n = len(do)
    mitad = n // 2

    if n % 2 == 0:
        return (do[mitad - 1] + do[mitad]) / 2
    else:
        return do[mitad]